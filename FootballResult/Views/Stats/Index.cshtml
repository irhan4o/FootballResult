@model IEnumerable<FootballResult.Models.Data.Stats>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.resultFirstTeam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.resultOtherTeam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Corner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsFinished)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.resultFirstTeam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.resultOtherTeam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Corner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsFinished)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
